# Mnemox 启动指南

> 更新日期：2026-08-05。本文是 Windows 本地启动的唯一入口；产品配置、Docker 和开发约定见根目录 `README.md`。

## 目录
- [前置条件与首次准备](#前置条件与首次准备)
- [快速启动](#快速启动)
- [手动启动](#手动启动)
- [数据库迁移](#数据库迁移)
- [停止服务](#停止服务)
- [常见问题](#常见问题)

---

## 前置条件与首次准备

本地开发需要：

- Python 3.10+
- Node.js 18+ LTS（含 npm）
- Git（仅用于获取代码）

检查版本：

```powershell
python --version
node --version
npm --version
```

双击 `start.bat` 会自动创建 `backend\venv`、安装依赖并生成本地 `backend\.env`。手动启动时，先在项目根目录执行：

```powershell
if (-not (Test-Path backend\.env)) { Copy-Item backend\env.example backend\.env }
if (-not (Test-Path backend\venv\Scripts\python.exe)) { python -m venv backend\venv }
& .\backend\venv\Scripts\python.exe -m pip install -r backend\requirements.txt
Set-Location frontend
npm install
Set-Location ..
```

编辑 `backend/.env`，至少保留随机 `SECRET_KEY`；AI Key 是可选项，不填也可以启动并使用基础页面。真实密钥、数据库和上传目录不要提交到 Git。

---

## 快速启动

### 方法一：使用批处理脚本（.bat）

**最简单！双击即可运行**

1. 找到项目根目录的 `start.bat` 文件
2. **双击运行**
3. 等待几秒，两个窗口会自动打开：
   - **后端窗口**：显示 API 服务日志
   - **前端窗口**：显示前端开发服务器
4. 浏览器会自动打开 `http://localhost:5173`

```
项目根目录\
├── start.bat          👈 双击这个文件
├── start.ps1
├── stop.bat
└── ...
```

---

### 方法二：使用 PowerShell 脚本（.ps1）

**功能更强大，彩色输出**

1. 找到项目根目录的 `start.ps1` 文件
2. **右键点击** → 选择 **"使用 PowerShell 运行"**
3. 如果提示权限错误，请先执行：
   ```powershell
   Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
   ```
4. 等待启动完成

---

## 手动启动

如果启动脚本无法使用，可以手动启动：

### 第一步：启动后端

打开终端（PowerShell 或 CMD），执行：

```bash
# 在项目根目录进入后端
cd backend

# 首次启动或升级后先执行迁移
.\venv\Scripts\python.exe run_migrations.py

# 启动后端服务
.\venv\Scripts\python.exe -m app.main
```

**看到以下输出说明后端启动成功**：
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

---

### 第二步：启动前端

**另开一个新终端**，执行：

```bash
# 在项目根目录进入前端
cd frontend

# 启动前端服务
npm run dev
```

**看到以下输出说明前端启动成功**：
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

---

### 第三步：打开浏览器

访问：**http://localhost:5173**

---

## 数据库迁移

数据库初始化和升级统一使用 `backend/run_migrations.py`，不要再直接用 `Base.metadata.create_all` 初始化生产数据库。

```powershell
cd backend
.\venv\Scripts\python.exe run_migrations.py
```

默认 SQLite 会执行轻量迁移；PostgreSQL 会执行 Alembic 版本迁移。当前 Alembic head 为 `20260804_03`。升级已有数据库前先做文件或数据库快照，并在生产发布窗口核对用户、事件、legacy 证据、状态、外键和回滚方案。

---

## 停止服务

### 方法一：使用停止脚本（推荐）

1. 双击运行 `stop.bat`
2. 自动停止所有前后端服务

---

### 方法二：手动停止

**直接关闭终端窗口** 或在终端中按 `Ctrl + C`

---

### 方法三：任务管理器

1. 按 `Ctrl + Shift + Esc` 打开任务管理器
2. 找到 `python.exe` 和 `node.exe` 进程
3. 结束进程

---

## 访问地址

启动后可以访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| 🎨 **前端界面** | http://localhost:5173 | 学习助手主界面 |
| 🔧 **后端 API** | http://localhost:8000 | API 服务 |
| 📚 **API 文档** | http://localhost:8000/docs | Swagger 交互式文档 |
| 📖 **备用文档** | http://localhost:8000/redoc | ReDoc 风格文档 |

---

## 常见问题

### Q1: 双击 `start.bat` 后窗口一闪而过？

**原因**：可能是 Python 或 Node.js 未安装或未添加到环境变量

**解决方法**：
1. 右键点击 `start.bat` → **"编辑"**
2. 在第一行下面添加 `pause`，查看错误信息
3. 或者手动启动查看详细错误

---

### Q2: 提示端口已被占用？

**错误信息**：
```
Address already in use
EADDRINUSE: address already in use :::8000
```

**解决方法**：

#### 方法一：使用停止脚本
```bash
双击 stop.bat
```

#### 方法二：手动查找并关闭占用端口的进程

**后端端口 8000**：
```powershell
# 查找占用 8000 端口的进程
netstat -ano | findstr :8000

# 结束进程（替换 PID 为实际进程号）
taskkill /F /PID <PID>
```

**前端端口 5173**：
```powershell
# 查找占用 5173 端口的进程
netstat -ano | findstr :5173

# 结束进程
taskkill /F /PID <PID>
```

---

### Q3: 数据库迁移失败或提示版本不是最新？

**解决方法**：

1. 先停止后端服务并备份 `backend/data/study.db`（SQLite）。
2. 确认在项目的 `backend` 目录执行 `python run_migrations.py`。
3. PostgreSQL 请确认 `DATABASE_URL` 指向目标数据库，并在升级前完成快照和演练。

不要删除生产数据库后重试，也不要用 `create_all` 绕过 Alembic。

---

### Q4: 后端启动报错 `ModuleNotFoundError`？

**错误信息**：
```
ModuleNotFoundError: No module named 'fastapi'
```

**解决方法**：安装后端依赖

```bash
cd backend
pip install -r requirements.txt
```

---

### Q5: 前端启动报错 `command not found: npm`？

**原因**：Node.js 未安装或未添加到环境变量

**解决方法**：
1. 安装 Node.js（已安装可跳过）
2. 安装前端依赖：
   ```bash
   cd frontend
   npm install
   ```

---

### Q6: 前端页面空白或报错？

**可能原因**：
- 后端未启动
- 前端依赖未安装
- 浏览器缓存问题

**解决方法**：
1. 确认后端和前端都已启动
2. 按 `Ctrl + Shift + R` 强制刷新浏览器
3. 检查浏览器控制台（F12）查看错误信息

---

### Q7: PowerShell 脚本无法运行？

**错误信息**：
```
无法加载文件，因为在此系统上禁止运行脚本
```

**解决方法**：修改执行策略

```powershell
# 以管理员身份打开 PowerShell，执行：
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

# 输入 Y 确认
```

---

### Q8: 浏览器没有自动打开？

**手动打开浏览器**，访问：http://localhost:5173

---

### Q9: 如何查看后端日志？

**方法一**：查看启动脚本打开的"学习助手-后端"窗口

**方法二**：手动启动后端并查看输出
```bash
cd backend
.\venv\Scripts\python.exe -m app.main
```

---

### Q10: 如何修改后端端口？

1. 手动启动时修改 `backend/.env` 中的 `PORT`，或在 Uvicorn 命令中显式传入 `--port`。
2. 使用 `start.bat` / `start.ps1` 时修改 `start.bat` 顶部的 `BACKEND_PORT`。
3. 同步调整前端代理或 CORS 配置后重启服务。

---

### Q11: 如何修改前端端口？

1. 手动启动时执行 `npm run dev -- --port 3000`。
2. 使用 `start.bat` 时修改脚本中的 `FRONTEND_PORT`，然后重启服务。

---

## 🎯 推荐工作流程

### 每天开始学习：
1. ✅ 双击 `start.bat` 启动系统
2. ✅ 等待浏览器自动打开
3. ✅ 开始愉快学习！

### 学习结束：
1. ✅ 关闭浏览器
2. ✅ 双击 `stop.bat` 停止服务
3. ✅ 或直接关闭两个终端窗口

---

## 💡 高级技巧

### 1. 开机自动启动

如果你想让学习助手开机自动启动：

1. 按 `Win + R`，输入 `shell:startup`
2. 将 `start.bat` 的快捷方式复制到这个文件夹

### 2. 创建桌面快捷方式

1. 右键点击 `start.bat`
2. 选择 **"发送到"** → **"桌面快捷方式"**
3. 可以右键快捷方式 → **"属性"**，修改图标

### 3. 使用 VSCode 集成终端

如果使用 VSCode 开发：
1. 打开项目
2. 按 `Ctrl + ` ` 打开终端
3. 点击终端右上角 **"+"** 旁的下拉箭头
4. 选择 **"拆分终端"**
5. 一个运行后端，一个运行前端

---

## 🔍 检查服务状态

### 检查后端是否运行：
访问：http://localhost:8000/docs

看到 Swagger 文档页面说明后端正常运行

### 检查前端是否运行：
访问：http://localhost:5173

看到学习助手界面说明前端正常运行

---

## 📞 需要帮助？

如果遇到其他问题：
1. 查看终端输出的错误信息
2. 检查 `backend/.env` 配置是否正确
3. 确认所有依赖已安装

---

## ✨ 祝学习愉快！

**现在就试试双击 `start.bat`，开始你的高效学习之旅吧！** 🚀
