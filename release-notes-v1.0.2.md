# Mnemox v1.0.2

发布时间：2026-05-23

## 本次重点更新

### 1. 桌面版登录记忆与自动登录

- 新增桌面安全凭据存储：Electron 通过系统加密存储保存用户名和密码，不再把密码放进浏览器本地存储。
- 登录页新增：
  - `记住密码`
  - `下次自动登录`
  - `清除已保存登录信息`
- 普通退出登录只会清掉当前 token，不会误删已保存的桌面凭据；如果需要彻底清除，可在登录页主动清除。

### 2. 托盘常驻与桌面提醒

- 桌面版关闭主窗口后不再直接退出，而是最小化到系统托盘后台常驻。
- 番茄钟开始、暂停、恢复、完成会同步到 Electron 主进程。
- 即使主窗口关闭，番茄钟到时仍会：
  - 弹出系统通知
  - 播放提示音
  - 点击通知可重新打开主窗口

### 3. API 错误提示与可诊断性增强

- 修复多处前端服务层把真实后端异常吞成 `null` / `[]` 的问题。
- 现在以下高频模块在失败时会明确提示，而不是显示成“空列表”或“没反应”：
  - 今日概览 / 学习仪表盘
  - AI 设置
  - Pomodoro
  - 对话与项目
  - 目标与任务
  - 复习中心
  - Agent 写入草稿 / 执行
  - 激励语录与相关设置
- 后端不可用时，会统一显示可读错误：`后端服务不可用，请确认 Mnemox 后端已启动`

### 4. 主学习流程稳定性收口

- `getMe()` 只在没有 token 时返回空；有 token 但接口失败时会暴露真实错误。
- 目标、任务、对话、复习、激励语录等高频流程的调用契约已统一为“服务层抛错，页面决定如何提示和兜底”。

## 回归验证

已完成以下验证：

- `frontend`
  - `npm test`
  - `npm run build`
  - `npm run lint`
- `desktop`
  - `npm test`
- `backend`
  - `python -m pytest tests -q`

## 对用户的直接影响

- 本地安装的桌面版现在可以稳定检测 GitHub Release 更新。
- 登录体验更顺手，桌面版支持真正的“记住密码 + 自动登录”。
- 关闭窗口后番茄钟不会丢提醒。
- 出问题时更容易看到真实原因，不会再经常误以为“只是没数据”。
