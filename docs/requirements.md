# Mnemox 产品需求基线

> 状态：维护中
>
> 基线日期：2026-07-26
>
> 当前发布版本：v1.3.0
> 对应代码分支：`main`

本文件定义 Mnemox 当前已经实现、正在收敛和下一阶段计划实现的产品范围。它是需求讨论与排期的入口；具体实现以 [技术文档](technical.md) 为准，执行状态以 [进度文档](progress.md) 为准，阶段顺序以 [路线图](roadmap.md) 为准。

## 1. 产品定位

Mnemox 是面向个人学习者的 AI 学习教练。它不以通用问答为终点，而是把资料、学习目标、任务、笔记、复习、错题、专注记录和用户反馈连接为可追溯的学习闭环。

**产品哲学**：产品要解决的是"知道却做不到"的知行差距。它把经过验证的学习科学方法（间隔效应、检索练习、行为设计、元认知）转化为用户的默认行为，衡量标准是行为转化而非回答质量。

**北极星指标**（新功能立项必须回答改善其中哪一项）：

1. 建议执行率：Agent/Coach 建议被实际执行的比例。
2. 中断恢复时长：番茄中断或学习断档后回到学习的时间。
3. 复习按时率：到期复习按时完成的比例。
4. 每周有效学习时段数。

产品应让用户清楚看到：

- 当前正在推进的学习目标和最小下一步。
- 建议依赖了哪些资料、笔记、行为数据和记忆。
- Agent 将要写入什么内容，以及写入前的确认入口。
- 学习行为如何影响复习、画像和后续建议。

## 2. 目标用户与场景

| 用户 | 核心诉求 | 典型场景 |
| --- | --- | --- |
| 备考或自学用户 | 将长期目标拆成可执行动作，持续推进 | 制定计划、开始学习、课后复盘 |
| 重度资料学习者 | 在自己的资料中问答、记录与复习 | 上传课程资料、围绕章节提问、整理笔记 |
| 容易中断的学习者 | 获得低打扰、可执行的提醒和补救建议 | 番茄钟中断、任务逾期、学习动力下降 |
| 有复盘习惯的用户 | 基于行为证据理解效率和薄弱点 | 查看 EDA、画像、错题和复习积压 |

## 3. 核心学习闭环

```text
资料/目标输入
  -> 计划与今日聚焦
  -> 对话、番茄钟、笔记、练习
  -> 行为事件与学习证据
  -> 画像、记忆、复习与风险识别
  -> Agent/Coach 给出草案或建议
  -> 用户确认、执行与反馈
  -> 下一轮个性化调整
```

其中，涉及创建或修改目标、任务、日计划、笔记、长期记忆等数据的 Agent 动作，必须先以草案呈现，用户确认后才能写入。

## 4. 已交付需求

### P0：学习主流程

| 模块 | 用户能力 | 状态 |
| --- | --- | --- |
| 账号与隔离 | 注册、登录、访问受保护页面；数据按用户隔离 | 已实现 |
| AI 对话工作台 | 流式对话、会话持久化、会话搜索、项目分组、模型覆盖，以及当前用户笔记上下文提示 | 已实现 |
| 学习资料与 RAG | 上传 PDF、Word、Markdown、TXT；资料解析、检索问答与关键词降级 | 已实现 |
| 学习目标与任务 | 创建目标、任务管理、周计划和每日计划 | 已实现 |
| 笔记 | Markdown 编辑、预览、分屏、关联目标/任务/资料/复习对象 | 已实现 |
| 番茄钟 | 计时、完成或中断记录、关联任务和统计 | 已实现 |
| 错题与复习 | 错题管理、复习计划、SM-2 风格记忆卡 | 已实现 |

### P1：个性化与主动支持

| 模块 | 用户能力 | 状态 |
| --- | --- | --- |
| 用户画像 | 根据学习行为汇总专注度、坚持度、计划执行和高效时段 | 已实现 |
| 双层记忆 | 会话摘要、长期记忆候选、确认/锁定/忽略与 Core Profile | 已实现 |
| 自主学习 Agent | 汇总目标、任务、错题、复习、笔记和记忆，生成下一步建议与行动草案 | 已实现 |
| Coach Kernel | 根据事件、偏好、冷却和每日上限生成低打扰提醒 | 已实现 |
| 主动干预 | 每日学习报告、风险判断和可执行建议 | 已实现 |
| 数据分析 | 学习趋势、时段效率、热力图、停止原因和能力雷达 | 已实现 |
| 联网搜索 | Hosted search、Tavily、应用层搜索及 DuckDuckGo/Bing 兜底 | 已实现 |

### P2：产品体验与交付

| 模块 | 用户能力 | 状态 |
| --- | --- | --- |
| 多模型设置 | 多供应商、场景路由、连接测试、token 预算与密钥加密保存 | 已实现 |
| 离线优先 | 笔记、目标、任务、错题、Anki 卡片的 IndexedDB 缓存与同步队列 | 已实现 |
| 桌面版 | Windows Electron 壳、更新检查、桌面通知桥接和安装包发布配置 | 已实现 |
| 个性化界面 | 主题、背景图、侧栏调整、提示词与激励语录设置 | 已实现 |

## 5. 质量与约束需求

### 5.1 安全与隐私

- API 必须基于登录态识别用户；所有用户数据读取、更新、删除都必须绑定当前用户。
- 上传文件必须限制类型和大小，文件访问必须通过认证并防止路径穿越。
- AI Provider Key、Tavily Key 等敏感配置不得明文存入前端或提交到仓库。
- 资料、笔记、网页搜索结果和工具输出均属于不可信上下文，不能覆盖系统指令或绕过用户确认。

### 5.2 可靠性

- 未配置 AI Key、Embedding 服务异常、搜索服务失败或 Agent Planner 超时时，核心学习流程不能整体失败。
- RAG 语义检索不可用时必须回退到关键词检索，并向用户提示当前检索状态。
- 流式对话的后处理步骤失败不能回滚已持久化的聊天内容。
- 离线操作应被排队、可观察、可重试；冲突需要有明确的用户处理策略。

### 5.3 可用性

- 用户应在主工作区快速找到当前目标、今日动作和到期复习。
- Agent 建议应展示来源证据、风险原因和预期写入结果。
- 界面应支持键盘操作、清晰焦点状态、足够对比度和窄屏布局。
- 自动化提醒默认低打扰，遵循用户偏好、免打扰时段、冷却时间与每日上限。

## 6. 下一阶段需求优先级

> 阶段顺序、验收标准和冻结清单的权威来源是 [路线图](roadmap.md)；本节仅概述需求含义。架构决策依据见 [2026-07-26 决策文档](superpowers/specs/2026-07-26-knowledge-layer-context-substrate-agent-architecture.md)。

### 立即（小胜利轨道）

1. 自引激励收尾：低动力场景下引用用户自己的笔记原文（注明出处），含防疲劳冷却与采纳反馈。
2. FSRS 复习调度：以 `py-fsrs` 替换手写 SM-2 风格调度，保留存量复习历史。

### P0：Beta 稳定化

1. 系统性完成多用户授权审计，重点检查详情、更新、删除和文件访问接口。
2. 统一 Prompt Injection 防护层，为资料、笔记、搜索和工具结果建立明确的不可信上下文边界。
3. 在前端展示 RAG 当前状态、关键词回退和最近 Embedding 错误。
4. 补齐端到端冒烟验证，覆盖登录、对话、上传、目标任务、笔记、复习和 Agent 草案确认。
5. 仓库卫生：清理 `release/` 历史快照，规范提交分支状态。

### P1：知识层（Phase 1）

1. 概念图谱：知识点成为一等实体，资料/笔记/错题/卡片通过概念互联，支持先修与相关关系。
2. 检索底座统一：`ContextStore` 接口收敛 RAG、笔记、记忆三套检索；OpenViking spike 决定实现选型。
3. Obsidian vault 持续同步：监听式增量导入替代一次性导入。
4. 联想引擎：学习新概念时自动联想旧知识（旧笔记、旧错题），经 Coach 低打扰治理呈现。
5. 概念级掌握度与薄弱点下钻：错题 → 先修缺口 → 资料小节 → 针对性卡片/任务。

### P2：Agent 升级（Phase 2）

1. AgentKernel：多步工具调用循环替代一次性 Planner，写入仍走草案确认。
2. 后台自主性：调度器驱动 Coach 触发评估、夜间知识巩固与知识周报（写回 Obsidian）。
3. 干预效果自学习：触达-采纳-行为链路量化，驱动技能与时机选择；北极星指标看板。

### P3：生态与公开试用（Phase 3）

1. MCP Server：向外部 AI 客户端暴露画像、图谱、错题与复习状态。
2. 语音：TTS 激励朗读 → STT 输入 → 全双工对话，依次评估推进。
3. 一键 Demo 数据、发布自动化；AnkiConnect 集成评估。
4. 在隐私与授权边界明确后，评估学习伙伴、共同番茄钟和轻量监督功能。

### 持续项（随相关阶段推进，不单独立项）

- 搜索来源可信度排序、去重、缓存与引用展示。
- 离线同步冲突检测、差异展示与用户决策入口。
- 上下文 token 预算分层：由 `ContextStore` 分层加载能力承接（见决策文档 D3）。

## 7. 非目标

- 不把 Mnemox 做成只追求对话能力的通用 AI 聊天产品。
- 不与成熟单点工具正面竞争：Markdown 编辑器功能冻结（保留现状），复习界面长期可交给 Anki 等成熟软件，Mnemox 聚焦图谱、行为数据与教练策略。
- 不引入通用 agent 框架/运行时（`earendil-works/pi`、LangGraph 等），理由见决策文档 D4。
- 不实现任何站点音视频下载功能；音频需求以本地导入与官方嵌入播放器满足。
- 不在没有明确用户授权和数据隔离设计前上线多人社交、公开资料或跨用户推荐。
- 不以增加自动写入数量为目标；任何高影响写入都应保留草案和确认步骤。
- 不以大规模生产部署为当前阶段目标，当前优先级是单用户/小范围 Beta 的稳定性、可解释性和学习闭环质量。
