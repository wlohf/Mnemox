# 2026-08-04 至 2026-08-05

## 本周期目标

在学习者模型首个后端切片之后，补齐事件投影、可重放边界和数据库迁移收口，并清理会误导后续开发的旧文档。

当前状态：数据库迁移演练、outbox、范围重放、学习者模型 API、前端证据下钻和离线校准基线均已完成；本周期切片正确性已收口，进入真实数据积累和生产化阶段。

## 已完成内容

- 新增 `projection_outbox` 模型、Alembic 修订 `20260804_02` 和兼容对齐修订 `20260804_03`。
- `LearningEvent` 与 outbox 在同一事务写入；支持幂等键、状态、重试、错误、模型版本、崩溃恢复和用户/概念/时间范围重放。
- 复习事件通过 outbox 投影到学习者证据；Review/Anki 请求内只消费当前事件，章节/卡片自身的调度字段仍在领域事务中更新。证据唯一键保证重复消费收敛。
- 新增 `/api/learner-model` 状态、证据分页、人工修正/撤销、单概念/批量重算、重放和 outbox 处理接口，全部按用户隔离。
- 合并并修正 Windows 启动、依赖准备和数据库迁移说明；`启动指南.md` 成为本地启动入口。
- 删除 29 份被现行基线替代的早期系统设计、Coach/RAG 方案、一次性实施计划、旧 UI 记录、旧修复记录和 `v1.0.3`–`v1.2.0` 发布说明；完整历史仍可通过 Git 追溯。
- 将 2026-07-26 架构文档压缩为仍有效结论摘要，移除已被 2026-08-03 决策覆盖的旧 schema、旧选型和实施快照。
- 默认 SQLite 已在线备份并升级到 `20260804_03`；备份位于 `backend/data/backups/`。升级前快照保留了已知的 `ai_routing_settings -> ai_provider_settings` 外键漂移，升级后的数据库通过 SQLite integrity/foreign-key 检查。
- PostgreSQL 16 一次性演练库已从 v1.3 基线升级到 `20260804_03`，验证用户、概念、legacy 证据、状态数量、outbox 运行投影和 `alembic check`。
- Review/Anki 完成复习现在只消费当前 `source_event_id` 的 outbox；失败任务保留 `failed/attempts/last_error/available_at`，可由恢复入口重试。
- 前端 `/mastery` 已支持概念证据/章节分布双标签、掌握度/置信度/遗忘风险/可靠度/模型版本、直接/间接/人工/legacy 来源筛选、解释下钻和人工修正撤销。
- 新增留后法离线回放和三个模型版本对比；真实 SQLite 库当前 `0 evidence / 0 holdout cases`，报告明确建议 `collect_more_data`，不晋级候选。

## 涉及文件

- `backend/app/models/learner_model.py`
- `backend/app/services/learner_model_service.py`
- `backend/app/services/projection_outbox_service.py`
- `backend/app/services/learning_event_service.py`
- `backend/app/routers/learner_model.py`
- `backend/alembic/versions/20260804_01_learner_model_boundary.py`
- `backend/alembic/versions/20260804_02_projection_outbox.py`
- `backend/alembic/versions/20260804_03_legacy_schema_alignment.py`
- `backend/tests/test_learner_model_service.py`、`test_projection_outbox.py`、`test_learner_model_api.py`、`test_schema_migration.py`
- `README.md`、`启动指南.md`、`docs/README.md`、`docs/updates/README.md`
- 删除的旧文档均已从导航和正文引用中移除；历史版本仍可通过 Git 追溯。

## 数据库演练证据

- 默认 SQLite：16 users / 19 learning events / 0 concepts，因此 legacy evidence/state 均为 0；projection outbox 也为 0，因为 schema 迁移不自动为历史事件创建任务，历史投影通过显式 replay 触发。升级前快照仍可观察到旧的 `ai_routing_settings -> ai_provider_settings` foreign-key mismatch；升级后 integrity `ok`、foreign-key violations `0`、`alembic check` 干净。
- SQLite 收口备份：`backend/data/backups/study-pre-slice-close-20260805-085415.db`，SHA256 `28AF023FD4950BE191389B57C097698653BC3E2AEB0937907B04CD0DD3221AB8`，与当前 `study.db` 一致。
- 完整数据库演练与恢复/回滚步骤见 `docs/database-rehearsal-2026-08-05.md`。
- PostgreSQL 16：2 users / 2 concepts / 2 learning events / 2 legacy evidence / 2 states；legacy score 为 `0.725`、`0.41`，reliability 为 `0.35`；outbox 在线消费 `1 processed`。
- PostgreSQL 用户、概念和事件外键均为 `ON DELETE CASCADE`；演练容器已删除。正式环境回滚使用升级前快照并同步回退应用版本，不依赖自动 downgrade。

## 验证

- 最新聚焦验证：learner model + API + calibration 合计 `23 passed`；projection outbox `9 passed`（含 525 条分页重放和事件级在线消费）；schema/learning event/north-star `9 passed`
- 上一相关回归：learning event / review / critical path 合计 `16 passed`
- Python compile、SQLite/PostgreSQL `alembic check`、PostgreSQL offline DDL 和在线投影消费通过
- PostgreSQL 演练：`2 users / 2 concepts / 2 legacy evidence / 2 states`，投影消费 `1 processed`
- 文档相对链接检查通过，无缺失目标。
- 已删除文件名的全仓库引用检查通过，无悬空引用。
- Markdown 代码围栏、行尾空白和 `git diff --check` 通过；仅有仓库既有的 LF/CRLF 转换提示。

前端最新验证：`21 files / 66 tests passed`、生产构建和 lint 通过；390px 视口 `scrollWidth=390` 且无截断文本。

## 后续事项

- 积累至少 50 个 holdout case 后重新运行 `backend/calibrate_learner_model.py`，再决定是否评估候选模型。
- 补常驻 outbox worker、失败队列监控/告警和其他领域投影；请求内事件级消费继续作为低延迟路径。
- 正式 PostgreSQL 发布升级仍需按发布窗口执行快照、升级、核对和回滚预案。
- 兼容周期结束并完成校准验收后，才规划移除 `Concept.mastery`；Phase 2 继续等待。
