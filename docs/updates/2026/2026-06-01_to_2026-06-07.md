# 更新记录：2026-06-01 至 2026-06-07

## 本周期目标

- 修正番茄工作法被设置面板意外打断的问题。
- 为番茄工作法补一个独立页面，提升桌面端展示体验。
- 把计划页和笔记页调整为更接近 Obsidian 的文档工作台。
- 让右侧边栏“今日任务”直接展示具体任务内容，而不是过于概括的摘要。
- 补齐对应的回归测试和页面自测。

## 已完成

### 0. 1.0.8 发布修复

- 修复 GPT / OpenAI-compatible 中转在 Responses 流式模式下的联网搜索路径，优先使用 hosted `web_search`。
- 明确搜索类请求会强制触发 hosted web search；普通问题保持 `auto`。
- hosted tool 不兼容时自动回退到 Responses function tool，再回退到 Chat Completions 流式工具调用和本地搜索。
- 本地 `web_search` 增加 Bing RSS / Bing HTML 兜底，减少 DuckDuckGo HTML 空结果导致的搜索失败。
- 图片上传默认上限调整为 50MB，并让番茄背景图走后端图片上传与鉴权 URL。
- 统计弹窗时长显示统一为 `h` / `m` 紧凑格式。

### 1. 功能 / 体验

- 番茄工作法设置保存时不再打断已开始的专注计时，避免用户仅调整设置就丢失当前状态。
- 新增独立的番茄工作法页面，提供更完整的专注视图、任务展示和左上角激励语句。
- 左侧导航的番茄入口改为跳转独立页面，避免计时入口和设置入口混在侧栏卡片里。
- 右侧边栏“今日任务”改为直接展示当天具体任务，并支持跳转到当天计划文档。
- 计划页从弹窗输入改为中间工作区文档编辑，用户可以直接围绕当天计划进行连续编辑。
- 笔记页调整为更接近 Obsidian 的三栏结构，按文件夹与笔记文件组织内容。

### 2. 修复 / 稳定性

- 修复停止专注计时且不进入休息时，`currentTask` 未被清空的问题。
- 修复切换到空文件夹时，笔记编辑区仍残留上一条笔记内容的问题。

### 3. 测试 / 验证

- 为计划页文档工作台补充测试，确认不再走弹窗输入流程。
- 为笔记页文件夹切换补充测试，确认空目录时会清空当前笔记。
- 为番茄状态补充测试，确认停止时任务状态同步清理。
- 新增浏览器级自测脚本，覆盖番茄页、计划页、笔记页和右侧任务跳转链路。

## 涉及文件

- `frontend/src/App.tsx`
- `frontend/src/components/PomodoroTicker.tsx`
- `frontend/src/components/Layout/GlobalNavRail.tsx`
- `frontend/src/components/Layout/ObsidianLayout.tsx`
- `frontend/src/pages/PomodoroPage.tsx`
- `frontend/src/pages/PlansPage.tsx`
- `frontend/src/pages/NotesPage.tsx`
- `frontend/src/stores/pomodoroStore.ts`
- `frontend/src/stores/pomodoroStore.test.ts`
- `frontend/src/pages/PlansPage.test.tsx`
- `frontend/src/pages/NotesPage.test.tsx`
- `frontend/src/index.css`
- `frontend/scripts/selftest-ui.mjs`
- `frontend/public/pomodoro-sky.jpg`

## 验证结果

- `backend\venv\Scripts\python.exe -m pytest` 通过，覆盖 71 个测试用例。
- `frontend\npm run build` 通过。
- 真实中转 smoke test 通过，确认 Responses hosted `web_search` 能触发联网并返回来源 URL。
- `npm run test` 通过，覆盖 13 个测试文件、41 个测试用例。
- `npm run build` 通过。
- 页面级自测通过，确认以下链路正常：
  - `pomodoro-page-ok`
  - `plans-workbench-ok`
  - `notes-workbench-ok`
  - `sidebar-task-link-ok`

## 已知限制 / 后续事项

- 后续新增功能或优化时，不再继续扩写 `UI-UPDATE.md` 这类单篇历史文档，优先写入 `docs/updates/`。
- 若一周内需求密度过高，可提前结束当前文件，按 3 到 5 天重新开一篇。
- 发版说明仍可保留在根目录 `release-notes-*.md`，但具体实现过程和验证记录统一维护在这里。
