# 2026-08-22：概念图谱与可解释学习推荐闭环

## 本轮交付

- 为概念、关系、别名、资料摘录和审核状态建立 SQL 规范契约及操作审计；Alembic / SQLite head 更新为 `20260822_11`。
- 资料上传自动抽取候选概念、括号别名和明确先修关系，无需模型请求；资料更新与删除同步清除旧版本来源及自动派生关系。
- 支持人工确认/拒绝、别名、改名、合并、拆分、删除和来源证据下钻；合并迁移学习证据、错题、挂接、关系和投影队列。
- 关系覆盖先修、组成、相关、示例与混淆，禁止跨用户引用与先修循环；只有确认后的关系参与先修缺口。
- 错题创建自动回填概念和答错证据；错题复习修复失效的旧 `_sm2_update` 导入，统一切换到 FSRS 优先调度并写入复习证据。
- 学习状态增加答题、正确和提示计数，事件投影扩展到解释、迁移应用、提示、重复提问、中断与恢复。
- 提供只读、可解释的到期复习、先修补缺、无提示回忆、针对性练习和目标推进推荐；公开分项得分、目标、证据及 FSRS 来源。
- `/mastery` 现可查看待确认概念、别名、来源摘录、先修缺口、身份治理和推荐原因。

## 技术边界

- SQLite/PostgreSQL 和原始资料保持唯一规范来源；Neo4j 不进入运行时依赖。
- 推荐不会直接修改用户计划或目标，后续执行动作仍必须通过草案确认。
- 正式发布版本仍为 `v1.3.0`；本次开发基线推进不等于新 tag、Release 或 Windows 安装包。

决策细节见 [概念图谱与学习推荐 ADR](../../superpowers/specs/2026-08-22-concept-graph-learning-recommendations-adr.md)。下一主线切换为 SQL 时态记忆冲突、替代、失效与纠错闭环。
