# 更新记录：2026-08-19

## 本周期目标

- 将 `main` 与历史功能分支中的有效增量收敛到单一集成基线。
- 解决合并冲突、跨平台换行漂移和现行文档中的事实性漂移。

## 整合范围

- 合并基线：`main@410cd3c` 与 `feature/phase2-agent-upgrade@5db1035`。
- 有效功能增量：聊天笔记 ContextStore、Coach 联想归因、Vault 同步安全边界、人工/自动 SQL 记忆声明。
- 文档范围：根 README、文档导航、需求、技术、进度、路线图、现行 ADR 与周期更新。
- 明确不做：新版本号、tag、安装包、候选技术采纳、生产数据库升级或提前启动 Phase 2。

## 冲突与漂移处理

- `docs/progress.md` 与 `docs/roadmap.md` 的冲突按“08-17 已验证代码事实 + 08-13 现行架构边界”合并，没有用任一侧全文覆盖另一侧。
- 聊天笔记首条 ContextStore 路径统一标为“接口迁移主体完成”，同时保留独立投影、混合召回、版本更新/删除残留和质量集等未完成项。
- Vault、Coach 和记忆声明状态与 08-17 验证记录对齐；Qdrant、Neo4j、Graphiti、LangGraph 继续保持候选或未评估状态。
- Alembic 当前 head 从文档中的旧值 `20260812_06` 校准为代码中的 `20260816_09`。
- 统一受 `.gitattributes` 管理的文本换行，避免 Windows 与 Linux checkout 反复制造纯换行差异。

## 验证

- 合并完整性：`MERGE_HEAD` 与 `origin/main@410cd3c` 一致；两个内容冲突均已解决，无残留冲突标记，`git diff --check` 通过。
- 迁移与文档：11 个 Alembic 修订形成单一 head `20260816_09`，无缺失父修订；12 份变更文档的本地链接检查通过。
- 后端：功能分支逻辑在合并前已覆盖完整回归；本次最终树相对该分支仅修改 ContextStore 模块说明，`python3 -m compileall -q backend/app backend/tests` 通过。当前沙箱无法下载临时 pytest 依赖，未把这一限制伪装成新的全量通过结果。
- 前端：`24 files / 72 tests passed`；`npm run build` 与 `npm run lint` 通过。
- 桌面端：Linux Node runner 为 `19 passed / 2 failed`；两项失败均来自 Windows 盘符在 POSIX `path` 下的解释差异，需 Windows CI/桌面门禁确认。
- 正式 PostgreSQL 多实例升级、真实浏览器/Windows 桌面 E2E：仍保留到发布窗口，不以本地回归替代。

## 后续事项

1. 积累至少 50 个 holdout case 后运行学习者模型离线校准。
2. 为 ContextStore 补可重建检索投影、版本更新/删除残留、质量集和更广泛的 `ingest/forget` 生命周期。
3. 使用真实多 Vault 数据验证冲突/删除策略，并完成正式 PostgreSQL `20260816_09` 多实例升级演练。
4. 积累 Coach shown/accepted/completed 与行为变化样本，验证归因和阈值。
5. 验证记忆声明的删除、纠错和冲突语义后，再决定是否开展 Graphiti Spike。
