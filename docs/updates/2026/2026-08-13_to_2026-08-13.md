# 更新记录：2026-08-13

## 本周期目标

将历史功能分支中的 Phase 0/Phase 1 有效实现、验证证据和现行文档整合进 `main`，同时收口旧分支与跨平台换行问题；不扩大 Phase 2 范围，不冒充一次新版本发布。

## 指标与范围

- 对应北极星指标：本次是工程基线整理，不直接改变产品行为指标。
- 本周期范围：分支关系核对、主线整合、README/技术/进度/路线图同步、换行规范、关键回归。
- 明确不做：不发布新安装包，不创建版本 tag，不执行生产 PostgreSQL 升级，不把 AgentKernel 原型标记为 Phase 2 完成。

## 已完成

### 1. 主线与文档

- 将 `feature/phase2-agent-upgrade` 中已经验证的 Phase 0/Phase 1 实现整合进 `main`。
- README 明确区分“最新发布版 v1.3.0”和“post-v1.3 主线开发基线”，避免把合并误认为发布。
- 技术基线、项目进度、路线图、后端 README 和文档导航同步到 2026-08-13 状态。
- 路线图继续要求 Phase 2 等待 Phase 1 的数据、删除、重放和反馈边界验收。

### 2. 仓库卫生

- 新增 `.gitattributes`：源码和文档统一 LF，Windows 启动脚本保留 CRLF。
- 已合入的 `openhands/rag-agent-hardening` 分支退役，可在确认 PR #1 合并状态后删除远程分支。
- `vk/2ba2-` 是没有共同 Git 祖先的早期原型历史；已建立 `archive/vk-2ba2-2026-02-09` 归档分支，不使用 `--allow-unrelated-histories` 强行合并。原活动分支仍待从 GitHub Branches 页面删除。

### 3. 测试与验证

- 后端聚焦回归：AgentKernel、学习者模型、API、Outbox 运维、schema migration、ContextStore，共 `61 passed`。
- 前端：`22` 个测试文件、`67` 个测试通过。
- 前端生产构建和 ESLint 通过。
- `git diff --check` 通过。

### 4. 笔记、检索与记忆边界决策（仅文档）

- 明确笔记采用“SQL 原文 -> ContextStore 可重建检索投影 -> 概念/记忆/学习证据派生理解”的三层逻辑存储，不要求立即部署三套数据库。
- 明确查询采用“路由与范围过滤 -> 关键词/语义混合召回 -> 重排与 L0/L1/L2 分层加载”的三阶段检索。
- 整篇笔记不自动转成长记忆；只有描述用户目标、偏好、困惑或阶段变化且可回链原文的片段才进入待审核记忆候选。
- 笔记中的自述不直接提高掌握度；学习者状态继续由练习、回忆、解释、应用、复习和人工修正等证据决定。
- 下一条代码切片确定为聊天笔记检索迁入 `ContextStore`；本次只更新文档，没有实现或宣称代码迁移完成。

## 失败、降级与数据变更

- 本轮不修改业务数据；Alembic head 继续为 `20260812_06`。
- 正式 PostgreSQL 多实例并发验收仍保留给独立发布窗口。
- 真实浏览器/桌面端的草案确认执行 E2E 尚未完成，不因本次主线整合改变状态。

## 发布与回滚

- 最新发布版本仍为 `v1.3.0`；本次没有新的 Release、tag 或安装包。
- 如需整体撤回，优先回退主线整合 PR；数据库升级和应用发布必须继续使用各自独立的回滚预案。

## 后续事项

1. 积累至少 50 个真实 holdout case 后运行学习者模型校准。
2. 迁移一条真实资料或笔记检索链路到 `ContextStore`。
3. 收口 Obsidian 删除/冲突和 Coach shown/accepted/executed 反馈闭环。
4. 完成正式 PostgreSQL 多实例和真实浏览器/桌面端发布验收后，再规划下一安装版本。
